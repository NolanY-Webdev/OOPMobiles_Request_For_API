import AutoMobile from './OOPMobiles.js';
export default class Bus extends AutoMobile{
  constructor (model, color, engineCyl, gas) {
    super(model, color, 2, engineCyl, 'maximum', 'manual', gas); {

    }
  }
}